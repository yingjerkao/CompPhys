def test():
    """A set of unit tests.
        Perform the following tests:
        There are 81 squares, 27 unitlist, and each square belongs 
        to three units and has 20 peers.  
        Check the peers['C2'] and units['C2'] with the correct answers.
        If all the tests are passed, print 'All tests passed.'       
    """
